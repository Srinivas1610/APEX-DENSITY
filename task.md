# Standalone Application Wrapper Checklist

- [x] Create silent VBScript launcher (`launch.vbs`)
- [x] Update Windows batch file launcher (`run.bat`)
- [x] Update PowerShell HTTP server (`server.ps1`) to monitor heartbeats and auto-exit
- [x] Implement client-side heartbeat pings in `app.js`
- [x] Embed Midsem & Endsem date inputs + countdowns beside subjects in `app.js`
- [x] Append responsive CSS media queries for sizing in `style.css`
- [x] Validate syntax and test silent window execution
